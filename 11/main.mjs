/**
 * @typedef Point
 * @property {number} x
 * @property {number} y
 */
/**
 * @typedef {{string: Point}} StarMap
 */

/**
 * @param {string} s 
 */
export const main = (s) => {
}

/**
 * @todo expand all empty rows and columns by duplicating them
 * 
 * @param {string} s
 * @returns {string} 
 */
const expansion = (s) => {
}

/**
 * @param {string} s
 * @returns {StarMap}
 */
const createMap = (s) =>{
}

/**
 * @todo measure the distance between two points only moving UDLR
 * @param {Point} p1 
 * @param {Point} p2
 * @returns {number}
 */
const distance = (p1, p2) => {
}